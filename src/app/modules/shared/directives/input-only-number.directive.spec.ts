import { InputOnlyNumberDirective } from './input-only-number.directive';

describe('InputOnlyNumberDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new InputOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
